{
  "provider": "python",
  "steps": {
    "install": {
      "commands": ["pip install requests"]
    }
  },
  "deploy": {
  "startCommand": "pip install requests && python data_collector.py"
  }
}

